---
- name: Mantenimiento automático de Guacamole
  hosts: guacamole
  become: yes
  tasks:

    - name: Actualizar lista de paquetes
      apt:
        update_cache: yes

    - name: Actualizar todos los paquetes del sistema
      apt:
        upgrade: dist
        autoremove: yes
        autoclean: yes

    - name: Reiniciar servicio de Tomcat (Guacamole)
      service:
        name: tomcat9
        state: restarted
      ignore_errors: yes

    - name: Reiniciar servicio MySQL (si existe)
      service:
        name: mysql
        state: restarted
      ignore_errors: yes

    - name: Verificar estado de Tomcat
      command: systemctl is-active tomcat9
      register: tomcat_status
      ignore_errors: yes

    - name: Mostrar estado de Tomcat en salida
      debug:
        msg: "Estado de Tomcat: {{ tomcat_status.stdout | default('No se pudo obtener estado') }}"

    - name: Crear carpeta de backups si no existe
      file:
        path: /opt/guacamole_backups
        state: directory
        mode: '0755'

    - name: Buscar archivo de configuración de Guacamole
      find:
        paths:
          - /etc/guacamole
          - /usr/share/tomcat9/.guacamole
          - /etc
        patterns: "guacamole.properties"
      register: guac_config

    - name: Respaldar archivo de configuración (si existe)
      copy:
        src: "{{ guac_config.files[0].path }}"
        dest: "/opt/guacamole_backups/guacamole.properties_{{ ansible_date_time.date }}"
        remote_src: yes
      when: guac_config.matched > 0
      ignore_errors: yes

    - name: Respaldar base de datos de Guacamole (si usa MySQL)
      command: mysqldump -u root -p'your_password' guacamole_db > /opt/guacamole_backups/guacamole_db_{{ ansible_date_time.date }}.sql
      ignore_errors: yes

    - name: Limpiar logs antiguos (más de 7 días)
      find:
        paths: /var/log
        age: 7d
        patterns: "*.log"
      register: old_logs

    - name: Eliminar logs antiguos
      file:
        path: "{{ item.path }}"
        state: absent
      loop: "{{ old_logs.files }}"
      when: old_logs.matched > 0

